services:
  - type: web
    name: ukhorsesbr-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: RAPIDAPI_KEY
        sync: false
